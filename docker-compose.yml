version: "3.8"

services:
  backend:
    build: ./server
    ports:
      - "4020:5000"
    restart: always
    environment:
      - NODE_ENV=production

  frontend:
    build: ./client
    ports:
      - "3010:80"
    restart: always
