server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Required for Google OAuth popup authentication
    add_header Cross-Origin-Opener-Policy "unsafe-none" always;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
